model_id: "microsoft/Phi-4-multimodal-instruct"
revision: "main"
task_type: "phi4mm-multimodal"
dtype: "float32"
trust_remote_code: true

inputs:
  prompts:
    - "Describe this image in detail."
  images:
    - "pipeline-cat-chonk.jpeg"

level: "L4+L5"

generation:
  max_new_tokens: 30
  do_sample: false

skip_reason: "Large model (14B). Requires GPU with >24GB VRAM for golden generation."
notes: "Phi-4-MM text + single image. Exercises vision encoder (SigLIP + HD transform) + projection + embedding mixer + decoder."
