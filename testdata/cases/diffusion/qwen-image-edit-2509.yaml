model_id: "Qwen/Qwen-Image-Edit-2509"
model_type: "qwen_image_edit"
revision: "d3968ef930e841f4c73640fb8afa3b306a78167e"
task_type: "image-to-image"
dtype: "bfloat16"
level: "L4+L5"
inputs:
  prompts:
    - "Make the cat wear a bright red scarf while preserving its pose."
  images:
    - "pipeline-cat-chonk.jpeg"
skip_reason: "The checked-in golden JSON is a reduced-config reference (seeded random weights), so this runner cannot be pointed at the 57.7 GB official checkpoint without comparing against the wrong reference. Reduced-config L4/L5 goldens live in src/mobius/models/qwen_image_test.py; official-weight validation is recorded under real_weight_validation below."
notes: "QwenImageEditPlusPipeline. The reduced golden encodes pipeline-cat-chonk.jpeg through a seeded diffusers Qwen Image VAE, packs the normalized posterior mode as source conditioning, runs the diffusers transformer and three FlowMatch Euler steps, and decodes the final edit latent. Mobius independently replays the same VAE-to-denoiser-to-decoder chain."
real_weight_validation:
  status: "passed"
  checkpoint: "Qwen/Qwen-Image-Edit-2509 @ d3968ef930e841f4c73640fb8afa3b306a78167e (public, ungated, 57.7 GB)"
  hardware: "NVIDIA H200 143 GB, CUDA 13.3, driver 580.105.08; 58 GiB peak GPU"
  upstream: "diffusers 0.39.0 QwenImageEditPlusPipeline, seed 42, 8 steps, true_cfg_scale 4.0, 1216x864 output"
  export: "mobius bfloat16 / CUDA EP; vae_encoder + vae_decoder + transformer + 3-stage text conditioning"
  component_parity:
    vae_encoder: "cos 0.99997, max abs 3.44e-2"
    vae_decoder: "53.31 dB PSNR"
    transformer_noise_pred: "cos 0.999974 (conditional) / 0.999981 (unconditional)"
    vision_encoder: "cos 0.99771"
    text_encoder: "cos 0.9636 isolated (bfloat16 cancellation in decoder layer 27; not a Mobius defect)"
  end_to_end:
    python_onnx_replay: "38.17 dB PSNR vs upstream image"
    onnx_genai_runtime: "37.10 dB PSNR / cos 0.99971 vs upstream, executed from Mobius-emitted inference_metadata.yaml"
    onnx_genai_runtime_onnx_conditioning: "33.49 dB PSNR / cos 0.99937 with ONNX-produced prompt embeds"
    batch_of_two: "row 0 34.79 dB / cos 0.99952; row 1 25.54 dB / cos 0.97753 (28.02 dB unpadded at batch 1)"
