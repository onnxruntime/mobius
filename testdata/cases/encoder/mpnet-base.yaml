model_id: "microsoft/mpnet-base"
model_type: "mpnet"
revision: "main"
task_type: "feature-extraction"
dtype: "float32"

inputs:
  prompts:
    - "The quick brown fox jumps over the lazy dog."

level: "L4"

skip_reason: "Architecture-specific differences from generic BERT (extra embeddings, missing params)."
notes: "MPNet base. Masked and permuted pre-training."
