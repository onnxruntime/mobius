model_id: "google/gemma-4-E2B-it"
revision: "main"
task_type: "image-text-to-text"
dtype: "float32"

inputs:
  prompts:
    - "Describe this image in detail."
  images:
    - "pipeline-cat-chonk.jpeg"

level: "L4+L5"

generation:
  max_new_tokens: 30
  do_sample: false

skip_reason: "Gated repo (google/gemma-4-E2B-it requires authentication). Audio path (Any-to-Any) also requires audio preprocessing pipeline not yet supported in test harness."
notes: "Gemma 4 E2B Any-to-Any. 4-model split: decoder + vision + audio + embedding. Pre-patchified vision input (pixel_values [B,N,3P^2] + pixel_position_ids). Conformer audio encoder."
