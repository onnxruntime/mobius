model_id: "google/gemma-4-12B"
model_type: "gemma4_unified"
revision: "main"
task_type: "image-text-to-text"
dtype: "float32"

inputs:
  prompts:
    - "Describe this image in detail."
  images:
    - "pipeline-cat-chonk.jpeg"

level: "L4+L5"

generation:
  max_new_tokens: 30
  do_sample: false

skip_reason: "Gated repo (google/gemma-4-12B requires authentication). Model is 12B — too large for CI."
notes: "Gemma 4 12B unified (encoder-free) Image-Text-to-Text. Raw-patch vision embedder instead of a SigLIP tower. Split: decoder + vision_encoder + embedding (audio_encoder added when config.audio is set). Vision-block bidirectional attention in the decoder."
