model_id: "microsoft/Phi-3.5-vision-instruct"
revision: "12b77fb40b63"
task_type: "image-text-to-text"
dtype: "float32"

inputs:
  prompts:
    - "What is shown in this image?"
  images:
    - "pipeline-cat-chonk.jpeg"

level: "L4+L5"

generation:
  max_new_tokens: 30
  do_sample: false

trust_remote_code: true
skip_reason: "Requires trust_remote_code; large 4.2B model — too large for CI golden data generation."
notes: "Phi-3.5 Vision. CLIP ViT-L/14-336 vision encoder + MLP projector + Phi-3.5 text decoder. Vision config stored in img_processor dict (non-standard)."
