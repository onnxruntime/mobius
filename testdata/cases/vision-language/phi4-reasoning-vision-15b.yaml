model_id: "microsoft/Phi-4-reasoning-vision-15B"
revision: "7df902e2fec3"
task_type: "image-text-to-text"
dtype: "float32"

inputs:
  prompts:
    - "What is shown in this image?"
  images:
    - "pipeline-cat-chonk.jpeg"

level: "L4+L5"

generation:
  max_new_tokens: 30
  do_sample: false

skip_reason: "Large 15B gated model — too large for CI golden data generation."
notes: "Phi-4 Reasoning Vision. SigLIP-2 SO400M vision encoder + mlp2x_gelu projector + Phi-4 text decoder (5120 hidden, 40 layers, GQA 40/10). Image token ID -200 (LLaVA convention) clamped before ONNX Gather."
