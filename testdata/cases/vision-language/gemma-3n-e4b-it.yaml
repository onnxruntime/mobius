model_id: "google/gemma-3n-E4B-it"
model_type: "gemma3n"
revision: "main"
task_type: "image-text-to-text"
dtype: "float32"

inputs:
  prompts:
    - "Describe this image in detail."
  images:
    - "pipeline-cat-chonk.jpeg"

level: "L4+L5"

generation:
  max_new_tokens: 30
  do_sample: false

notes: "Gemma 3n E4B multimodal. 4-model split: decoder + vision + audio + embedding. MobileNet-V5-300m vision tower (not SigLIP) at 768x768, USM conformer audio tower, and a Gemma3nMultimodalEmbedder soft path on each encoder's output. Decoder carries KV layer sharing (layers 20-34 reuse layer 18/19 KV) and per-layer activation sparsity."
