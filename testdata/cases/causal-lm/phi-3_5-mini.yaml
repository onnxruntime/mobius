model_id: "microsoft/Phi-3.5-mini-instruct"
model_type: "phi3"
revision: "2fe192450127e6a83f7441aef6e3ca586c338b77"
task_type: "text-generation"
dtype: "float32"

inputs:
  prompts:
    - "Here is my poem:"

level: "L4+L5"

generation:
  max_new_tokens: 20
  do_sample: false

trust_remote_code: false
notes: "Phi-3.5 with LongRoPE positional embeddings. trust_remote_code is
  false so the transformers-native phi3 implementation is used; the
  checkpoint's bundled modeling_phi3.py relies on cache APIs removed in
  transformers 5.x."
